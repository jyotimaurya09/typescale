import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const CopyValues = ({items}) => {
  return (
    <View>
      <Text>CopyValues</Text>
    </View>
  )
}

export default CopyValues

const styles = StyleSheet.create({})